<div class="p-8 bg-blue-800 min-h-screen">
  <!-- Card with DaisyUI -->
  <div class="card bordered max-w-md mx-auto">
    <figure>
      <img src="https://placekitten.com/400/250">
    </figure> 
    <div class="card-body">
      <h2 class="card-title">Hello, this is a DaisyUI card!</h2> 
      <p>This card is styled using both TailwindCSS and DaisyUI.</p>
      <!-- Button with DaisyUI -->
      <div class="card-actions">
        <button class="btn btn-primary">Click Me</button>
      </div>
<%= link_to "サインアウト", destroy_user_session_path, method: :delete, data: { turbo_method: :delete } %>

    </div>
  </div>
</div>

